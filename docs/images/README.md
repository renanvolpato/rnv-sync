# Screenshots

Add captured PNGs here:

- `dashboard.png` — account dashboard with quota
- `files.png` — file browser with cache status
- `conflicts.png` — conflict resolution screen

These are referenced by the root `README.md`. Capture at ~1280px wide,
light theme, with a demo account.
